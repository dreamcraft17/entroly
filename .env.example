# ===========================================
# ENTRO.LY - Environment Variables
# Copy to .env.local (local) atau set di Vercel / Railway (production)
# ===========================================

# --- Database (Railway PostgreSQL) ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://postgres:rXbMkqPANtoOePTbrCCbEaGleOwYooLK@postgres.railway.internal:5432/railway"

# --- Auth (required) ---
# Public key untuk verifikasi JWT dari SSO (PEM format, single line atau \n)
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# --- Auth URLs (optional, punya default) ---
# URL frontend/app ini (untuk redirect setelah login). Vercel: https://xxx.vercel.app | Railway: https://xxx.up.railway.app
AUTH_URL="https://your-app.vercel.app"
# URL SSO / login service (exposed ke client)
NEXT_PUBLIC_SSO_URL="https://sso.entro.ly"

# --- AI (optional - untuk fitur generate AI) ---
GOOGLE_API_KEY=""
